# PDF4QT architecture notes — placeholder

The full verified deep-dive (extension pattern, write-back paths, deps,
determinism, headless status — with file:line refs) lives in the live yolka
profile's copy of this skill. When you first need it, re-derive it from the
PDF4QT source (`git clone https://github.com/JakubMelka/PDF4QT`) following the
`codebase-architecture-research` workflow, then paste the findings back here so
the repo copy stays self-contained.
